var deb_ext_path="../extensions/ki_debug/";var deb_ext_refreshTimer=null;$(document).ready(function(){var deb_ext_resizeTimer=null;$(window).bind("resize",function(){if(deb_ext_resizeTimer){clearTimeout(deb_ext_resizeTimer);}deb_ext_resizeTimer=setTimeout(deb_ext_resize,500);});});
